<h1>Lista de posts:</h1>

{{#each posts}}
    <h1>{{dataValues.titulo}}</h1>
    <p>{{dataValues.conteudo}}</p>
    <strong>{{dataValues.createdAt}}</strong>
    <a href="/deletar/{{dataValues.id}}"><button>Detetar</button></a>
    <hr>
{{/each}}